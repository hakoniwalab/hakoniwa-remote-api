

/**
 * ROS message: hako_srv_msgs/AckEventResponse
 * Generated by hakoniwa-ros2pdu.
 */
export class AckEventResponse {
/** @type { number } */
    ack_code;


    constructor() {
        this.ack_code = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'ack_code'
            const field_val = this.ack_code;
            if (typeof field_val?.toDict === 'function') {
                d['ack_code'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['ack_code'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['ack_code'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { AckEventResponse }
     */
    static fromDict(d) {
        const obj = new AckEventResponse();
        if (d.hasOwnProperty('ack_code')) {
            obj.ack_code = d.ack_code;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { AckEventResponse }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
