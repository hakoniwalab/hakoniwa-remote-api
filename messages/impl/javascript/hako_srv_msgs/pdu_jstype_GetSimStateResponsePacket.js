import { GetSimStateResponse } from '../hako_srv_msgs/pdu_jstype_GetSimStateResponse.js';
import { ServiceResponseHeader } from '../hako_srv_msgs/pdu_jstype_ServiceResponseHeader.js';


/**
 * ROS message: hako_srv_msgs/GetSimStateResponsePacket
 * Generated by hakoniwa-ros2pdu.
 */
export class GetSimStateResponsePacket {
/** @type { ServiceResponseHeader } */
    header;
/** @type { GetSimStateResponse } */
    body;


    constructor() {
        this.header = new ServiceResponseHeader();
        this.body = new GetSimStateResponse();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'body'
            const field_val = this.body;
            if (typeof field_val?.toDict === 'function') {
                d['body'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['body'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['body'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { GetSimStateResponsePacket }
     */
    static fromDict(d) {
        const obj = new GetSimStateResponsePacket();
        if (d.hasOwnProperty('header')) {
            const field_class = ServiceResponseHeader;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('body')) {
            const field_class = GetSimStateResponse;
            if (d.body) {
                obj.body = field_class.fromDict(d.body);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { GetSimStateResponsePacket }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
