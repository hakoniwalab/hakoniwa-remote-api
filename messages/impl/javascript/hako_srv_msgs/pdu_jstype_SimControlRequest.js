

/**
 * ROS message: hako_srv_msgs/SimControlRequest
 * Generated by hakoniwa-ros2pdu.
 */
export class SimControlRequest {
/** @type { string } */
    name;
/** @type { number } */
    op;


    constructor() {
        this.name = "";
        this.op = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'name'
            const field_val = this.name;
            if (typeof field_val?.toDict === 'function') {
                d['name'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['name'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['name'] = field_val;
            }
        }
        {
            // handle field 'op'
            const field_val = this.op;
            if (typeof field_val?.toDict === 'function') {
                d['op'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['op'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['op'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { SimControlRequest }
     */
    static fromDict(d) {
        const obj = new SimControlRequest();
        if (d.hasOwnProperty('name')) {
            obj.name = d.name;
        }
        if (d.hasOwnProperty('op')) {
            obj.op = d.op;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { SimControlRequest }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
